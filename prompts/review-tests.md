---
name: review-tests
purpose: Перевірити, чи тестовий набір справді ловить регресії, а не просто зелений
tested-on: app/src/quote.test.ts після фіксу splitInstallments, 2026-09-12
version: 1
---

# Рев'ю тестового набору

## Роль

Рев'юер, який не довіряє зеленому прогону. Зелений набір означає лише, що
жоден із написаних тестів не впав — а не що код працює.

## Мета

Знайти, які зміни в продакшн-коді пройдуть повз наявні тести непоміченими.

## Контекст

- Тести: `{{шлях до тестів}}` — перевірено на `app/src/quote.test.ts`.
- Модуль, який вони покривають: `{{шлях}}` — перевірено на `app/src/quote.ts`.
- Раннер: `cd app && npm test`.
- Домен: суми — цілі числа в центах.

> Цей промпт рев'ює **тести**, а не продакшн-діф. Для рев'ю дифу перед мержем
> у бібліотеці є `prompts/review-pr.md` — інший об'єкт і інший метод.

## Обмеження

- НЕ пиши код тестів. Твоя робота — сказати, чого бракує.
- НЕ коментуй форматування, іменування змінних і стиль — лише покриття.
- Не пропонуй міняти продакшн-код.
- Не вигадуй зауваження, щоб набрати кількість.

## Acceptance criteria

- [ ] Для **кожного** тесту у файлі названо одну конкретну зміну в модулі, яка
      залишить цей тест зеленим (мутація, яку він не ловить) — або явно
      сказано «мутації не знайшов».
- [ ] Перелічено експортовані функції й гілки, на які немає жодного тесту,
      у форматі `файл:рядок`.
- [ ] Позначено тести, що перевіряють те саме, із зазначенням, який зайвий.
- [ ] Явно сказано, чи є в наборі тест, який зафіксував **поточну** поведінку
      замість очікуваної.
- [ ] Якщо набір достатній — так і написано, без вигаданих зауважень.

## Формат відповіді

Максимум 6 знахідок, відсортованих за серйозністю. Якщо їх більше — покажи
шість найсерйозніших і вкажи, скільки лишилось за межами списку.

Для кожної: `СЕРЙОЗНІСТЬ | файл:рядок | що не ловить | мутація-приклад`.

## Stop

Після списку зупинись. Не пиши відсутні тести — це окремий запит із окремим
рев'ю (`prompts/add-tests.md`).

---

## Результат прогону — 2026-09-12

- **Запуск:** на `app/src/quote.test.ts` у стані «32/32 зелені» — тобто набір,
  який на вигляд не має проблем.
- **Вийшло:** 6 знахідок із 9 (3 лишились за межами ліміту), і серед них
  одна **CRITICAL**, якої не бачив жоден із 32 тестів:
  `splitInstallments` не валідував `parts`. Фактично
  `splitInstallments(100, 0)` → `[]` (зникала вся сума),
  `splitInstallments(100, 2.5)` → `[40, 40]` (мінус 20 центів) — при тому,
  що JSDoc обіцяв «сума платежів точно дорівнює `totalCents`».
- **Мутація для кожного з 32 тестів** знайдена — жодного «мутації не знайшов».
  Найнеприємніша: back-loaded роздача залишку
  (`i < Math.abs(remainder)` → `i >= parts - Math.abs(remainder)`) лишає
  зеленими **чотири** параметризовані тести одразу.
- **Знайдено самопідтверджувальний тест.** Хелпер `parseMoney` у тестах
  реконструйований із реалізації `formatMoney`, тому round-trip «інваріант»
  стверджує «форматер узгоджений сам із собою», а не «формат правильний».
  Він не вбиває жодної мутації понад точкові тести.
- **Дію по знахідці виконано:** додано валідацію `parts`/`totalCents` і три
  тести на неї, 32 → 35. Рев'ю, за яким нічого не зробили, — це не рев'ю.
- **Що довелось поправити в промпті:** нічого. AC «для кожного тесту назвати
  мутацію, яка лишить його зеленим» виявився сильнішим, ніж очікувалось: він
  не дає відбутись загальними словами про покриття й змушує назвати
  конкретну зміну коду.
- **Повний вивід:** [`docs/prompt-runs.md`](../docs/prompt-runs.md#прогін-4).
